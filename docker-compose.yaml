services:
  react-app:
    build: .
    container_name: react-app
    ports:
      - "5051:80"
    volumes:
      - react-logs:/var/log/nginx
    networks:
      - react-network
    restart: unless-stopped

volumes:
  react-logs:

networks:
  react-network:
